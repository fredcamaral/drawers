{
	"extends": "../../tsconfig.base.json",
	"include": ["src"],
	"// jsx note": "the whole `src/tui` surface (the `./tui` plugin) is type-checked by tsconfig.tui.json (jsx:preserve + jsxImportSource @opentui/solid). It is excluded here because its `.tsx` files (and the `.ts` entry that lazily imports them) need the jsx setting the base config deliberately omits.",
	"exclude": ["src/tui"]
}
